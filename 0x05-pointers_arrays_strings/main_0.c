#include <stdio.h>

void puts_half(char *);

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "main.h";
	puts_half(srt);
	return (0);
}
