#include <stdio.h>

void _puts(char *);

/**
 * main - check the code for the Holberton School students.
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "Holberton!";
	_puts(str);
	return (0);
}
